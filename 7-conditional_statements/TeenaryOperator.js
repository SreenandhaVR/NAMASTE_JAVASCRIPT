// Ternary operator example
let temperature = 30;
let weather = temperature > 25 ? "It's a hot day!" : "It's a cool day!";
console.log(weather);

// Another example
let hasTicket = true;
let entry = hasTicket ? "Welcome to the show!" : "You need a ticket to enter.";
console.log(entry);
